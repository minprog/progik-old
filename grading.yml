grades:
    lab1:
        type: pass
    hw1:
        type: float
        subgrades:
            scope: integer
            correctness: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    hw2:
        type: float
        subgrades:
            scope: integer
            correctness: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    hw3:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            style: integer
        calculation: (scope * (3 * correctness + 3 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true

    oefen:
        type: float
        subgrades:
            points: integer
        calculation: (points / 12.0 * 9 + 1).round(1)
        hide_calculated: true
    hw4:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            style: integer
        calculation: (scope * (3 * correctness + 3 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    hw5:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    hw6:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    hw7:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    quiz:
        type: pass
        subgrades:
            points: integer
        calculation: (points / 22.0).round(1)
    resit:
        type: pass
        subgrades:
            points: integer

calculation:
    resit:
        resit: 0
        psets: 100
        passes: 0
    normal:
        quiz: 0
        psets: 100
        passes: 0

passes:
    required: true
    submits:
        lab1: 1
        hw1: 1

psets:
    submits:
        hw2: 1
        hw3: 1
        hw4: 1
        hw5: 1
        hw6: 1
        hw7: 1

quiz:
    required: true
    submits:
        quiz: 1

resit:
    required: true
    submits:
        resit: 1
